<p>
  <strong>Name:</strong>
  <%= @machine.name %>
</p>

<p>
  <strong>Hours Count:</strong>
  <%= @machine.last_hour_count %>
</p>

<%= link_to 'Edit', edit_machine_path(@machine) %> |
<%= link_to 'Back', machines_path %>
